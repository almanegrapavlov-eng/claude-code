// Collision skeleton.
//
// Resolves projectile-vs-enemy, enemy-vs-player, and gem pickup using circle
// overlaps (radii are kept smaller than the sprites so the game feels fair).
// Added alongside combat in a later step (see DESIGN.md).
export class CollisionSystem {
  // TODO (later step): run collision checks for the frame.
  update(dt, game) {}
}
