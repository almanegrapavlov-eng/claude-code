// Spawner skeleton.
//
// The wave / difficulty director (spawning enemies in a ring around the
// player and ramping difficulty over time) is implemented in a later step
// (see DESIGN.md). Nothing spawns yet.
export class Spawner {
  constructor() {
    this.enemies = [];
  }

  // TODO (later step): spawn and update enemies based on the run timer.
  update(dt, game) {}
}
